-- Guiby Monster · iOS Panel (Compact & Fixed Instant Kill + Auto Chest)
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UIS = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")

local Player = Players.LocalPlayer or Players.PlayerAdded:Wait()

-- ==================== DETEKSI PARENT GUI ====================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Guiby_iOS_Panel_" .. math.random(1000, 9999)
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

if gethui then
	ScreenGui.Parent = gethui()
else
	local success = pcall(function() ScreenGui.Parent = CoreGui end)
	if not success then
		ScreenGui.Parent = Player:WaitForChild("PlayerGui")
	end
end

-- ==================== TEMPLATE iOS PANEL (COMPACT SIZE) ====================
local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 260, 0, 165) -- Ukuran diperkecil tanpa list kosong di bawah
Main.Position = UDim2.new(0.5, -130, 0.4, -82)
Main.BackgroundColor3 = Color3.fromRGB(18,18,20)
Main.BackgroundTransparency = 0.15
Main.BorderSizePixel = 0
Main.Parent = ScreenGui

local Corner = Instance.new("UICorner", Main)
Corner.CornerRadius = UDim.new(0, 14)

local Stroke = Instance.new("UIStroke", Main)
Stroke.Color = Color3.fromRGB(50,50,55)
Stroke.Thickness = 1
Stroke.Transparency = 0.3

local TopBar = Instance.new("Frame", Main)
TopBar.Size = UDim2.new(1,0,0,32)
TopBar.BackgroundTransparency = 1

local Title = Instance.new("TextLabel", TopBar)
Title.Size = UDim2.new(1,-70,1,0)
Title.Position = UDim2.new(0,12,0,0)
Title.BackgroundTransparency = 1
Title.Text = "Guiby Monster"
Title.TextColor3 = Color3.fromRGB(240,240,245)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 13
Title.TextXAlignment = Enum.TextXAlignment.Left

local MinBtn = Instance.new("TextButton", TopBar)
MinBtn.Size = UDim2.new(0,20,0,20)
MinBtn.Position = UDim2.new(1,-50,0.5,-10)
MinBtn.Text = "−"
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 14
MinBtn.TextColor3 = Color3.new(1,1,1)
MinBtn.BackgroundColor3 = Color3.fromRGB(255,189,46)
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(1,0)

local CloseBtn = Instance.new("TextButton", TopBar)
CloseBtn.Size = UDim2.new(0,20,0,20)
CloseBtn.Position = UDim2.new(1,-25,0.5,-10)
CloseBtn.Text = "×"
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 14
CloseBtn.TextColor3 = Color3.new(1,1,1)
CloseBtn.BackgroundColor3 = Color3.fromRGB(255,95,86)
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(1,0)

local Content = Instance.new("Frame", Main)
Content.Size = UDim2.new(1,-16,1,-42)
Content.Position = UDim2.new(0,8,0,36)
Content.BackgroundColor3 = Color3.fromRGB(25,25,28)
Content.BackgroundTransparency = 0.2
Content.BorderSizePixel = 0

Instance.new("UICorner", Content).CornerRadius = UDim.new(0,10)
local ContentStroke = Instance.new("UIStroke", Content)
ContentStroke.Color = Color3.fromRGB(45,45,50)
ContentStroke.Thickness = 1
ContentStroke.Transparency = 0.4

-- ==================== DRAG SYSTEM ====================
local dragging, dragInput, dragStart, startPos

TopBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = Main.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then dragging = false end
		end)
	end
end)

TopBar.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
		dragInput = input
	end
end)

UIS.InputChanged:Connect(function(input)
	if input == dragInput and dragging then
		local delta = input.Position - dragStart
		Main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

-- ==================== MINIMIZE & CLOSE ====================
local Minimized = false
local FullSize = Main.Size

MinBtn.MouseButton1Click:Connect(function()
	Minimized = not Minimized
	if Minimized then
		Content.Visible = false
		TweenService:Create(Main, TweenInfo.new(0.3, Enum.EasingStyle.Quart), { Size = UDim2.new(0, 260, 0, 32) }):Play()
	else
		TweenService:Create(Main, TweenInfo.new(0.3, Enum.EasingStyle.Quart), { Size = FullSize }):Play()
		task.wait(0.15)
		Content.Visible = true
	end
end)

CloseBtn.MouseButton1Click:Connect(function()
	local tw = TweenService:Create(Main, TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.In), { Size = UDim2.new(0,0,0,0) })
	tw:Play()
	tw.Completed:Wait()
	ScreenGui:Destroy()
end)

-- ==================== LOGIC & FEATURES ====================
local enemiesFolder = nil

local function findEnemiesFolder()
	local found = nil
	local alives = Workspace:FindFirstChild("Alives")
	if alives then found = alives:FindFirstChild("Enemies") end
	if not found then
		for _, v in ipairs(Workspace:GetDescendants()) do
			if v:IsA("Folder") and v.Name == "Enemies" and v.Parent and v.Parent.Name == "Alives" then
				found = v
				break
			end
		end
	end
	return found
end

task.spawn(function()
	while true do
		enemiesFolder = findEnemiesFolder()
		task.wait(1)
	end
end)

local function getHumanoid(obj)
	local m = obj
	if obj:IsA("BasePart") then m = obj.Parent end
	if m and (m:IsA("Model") or m:IsA("Folder")) then
		return m:FindFirstChildWhichIsA("Humanoid")
	end
end

local hlts = {}
local playerHlts = {}
local espOn = false
local autoOn = false
local frozen = false
local autoChestOn = false

local function getEn()
	local t = {}
	if not enemiesFolder or not enemiesFolder.Parent then return t end
	for _, v in ipairs(enemiesFolder:GetChildren()) do
		local hh = getHumanoid(v)
		if hh and hh.Health > 0 then
			t[#t + 1] = {Name = v.Name, Model = v, Humanoid = hh}
		end
	end

	for _, d in ipairs(t) do
		if espOn and not hlts[d.Model] then
			local hl = Instance.new("Highlight", d.Model)
			hl.FillColor = Color3.fromRGB(180, 180, 180)
			hl.OutlineColor = Color3.new(1, 1, 1)
			hl.FillTransparency = 0.5
			hlts[d.Model] = hl
		elseif not espOn and hlts[d.Model] then
			hlts[d.Model]:Destroy()
			hlts[d.Model] = nil
		end
	end

	for m, hl in pairs(hlts) do
		local alive = false
		for _, d in ipairs(t) do if d.Model == m then alive = true break end end
		if not alive then hl:Destroy() hlts[m] = nil end
	end

	for _, otherPlayer in ipairs(Players:GetPlayers()) do
		if otherPlayer ~= Player and otherPlayer.Character then
			local char = otherPlayer.Character
			if espOn and not playerHlts[char] then
				local hl = Instance.new("Highlight", char)
				hl.FillColor = Color3.fromRGB(0, 255, 0)
				hl.OutlineColor = Color3.new(1, 1, 1)
				hl.FillTransparency = 0.5
				playerHlts[char] = hl
			elseif not espOn and playerHlts[char] then
				playerHlts[char]:Destroy()
				playerHlts[char] = nil
			end
		end
	end

	return t
end

-- Diperbaiki agar menembus pertahanan mob dan tetap memberikan EXP
local function killEn(d)
	if not d.Humanoid or d.Humanoid.Health <= 0 then return end
	local hh = d.Humanoid
	pcall(function()
		hh.MaxHealth = 100
		hh.Health = 0
		if d.Model and d.Model:IsA("Model") then
			d.Model:BreakJoints()
			pcall(function()
				for _, part in ipairs(d.Model:GetDescendants()) do
					if part:IsA("BasePart") then
						part.Velocity = Vector3.new(0, -50, 0)
					end
				end
			end)
		end
	end)
end

local function killAll()
	for _, d in ipairs(getEn()) do 
		killEn(d) 
	end
end

local function freezeAll()
	if not enemiesFolder then return end
	for _, v in ipairs(enemiesFolder:GetChildren()) do
		local hh = getHumanoid(v)
		if hh and hh.Health > 0 then
			hh.WalkSpeed = frozen and 0 or 16
			hh.JumpPower = frozen and 0 or 50
			for _, st in ipairs({
				Enum.HumanoidStateType.Running, Enum.HumanoidStateType.Jumping, Enum.HumanoidStateType.Falling,
				Enum.HumanoidStateType.Climbing, Enum.HumanoidStateType.Swimming,
			}) do
				hh:SetStateEnabled(st, not frozen)
			end
		end
	end
end

-- Diperbaiki: Auto Chest dengan bypass jarak total dan fireprompt instan
local function processChests()
	if not autoChestOn then return end
	pcall(function()
		local char = Player.Character
		local hrp = char and char:FindFirstChild("HumanoidRootPart")
		if not hrp then return end

		for _, obj in ipairs(Workspace:GetDescendants()) do
			if obj:IsA("ProximityPrompt") then
				obj.MaxActivationDistance = 999999
				obj.HoldDuration = 0
				local parentPart = obj.Parent
				if parentPart and parentPart:IsA("BasePart") then
					local oldPos = hrp.CFrame
					hrp.CFrame = parentPart.CFrame + Vector3.new(0, 2, 0)
					task.wait(0.05)
					if fireproximityprompt then
						fireproximityprompt(obj)
					end
					hrp.CFrame = oldPos
				else
					if fireproximityprompt then
						fireproximityprompt(obj)
					end
				end
			end
		end
	end)
end

-- ==================== UI BUTTONS & LAYOUT (COMPACT) ====================
local controlBar = Instance.new("Frame", Content)
controlBar.Size = UDim2.new(1,0,0,32)
controlBar.Position = UDim2.new(0,0,0,6)
controlBar.BackgroundTransparency = 1

local refreshBtn = Instance.new("TextButton", controlBar)
refreshBtn.Size = UDim2.new(0.48,0,1,0)
refreshBtn.BackgroundColor3 = Color3.fromRGB(45,45,50)
refreshBtn.Text = "Refresh"
refreshBtn.TextColor3 = Color3.new(1,1,1)
refreshBtn.Font = Enum.Font.GothamBold
refreshBtn.TextSize = 12
refreshBtn.BorderSizePixel = 0
Instance.new("UICorner", refreshBtn).CornerRadius = UDim.new(0,6)

local killAllBtn = Instance.new("TextButton", controlBar)
killAllBtn.Size = UDim2.new(0.48,0,1,0)
killAllBtn.Position = UDim2.new(0.52,0,0,0)
killAllBtn.BackgroundColor3 = Color3.fromRGB(235,75,70)
killAllBtn.Text = "Kill All"
killAllBtn.TextColor3 = Color3.new(1,1,1)
killAllBtn.Font = Enum.Font.GothamBold
killAllBtn.TextSize = 12
killAllBtn.BorderSizePixel = 0
Instance.new("UICorner", killAllBtn).CornerRadius = UDim.new(0,6)

local controlBar2 = Instance.new("Frame", Content)
controlBar2.Size = UDim2.new(1,0,0,32)
controlBar2.Position = UDim2.new(0,0,0,44)
controlBar2.BackgroundTransparency = 1

local espBtn = Instance.new("TextButton", controlBar2)
espBtn.Size = UDim2.new(0.48,0,1,0)
espBtn.BackgroundColor3 = Color3.fromRGB(45,45,50)
espBtn.Text = "ESP: OFF"
espBtn.TextColor3 = Color3.new(1,1,1)
espBtn.Font = Enum.Font.GothamBold
espBtn.TextSize = 11
espBtn.BorderSizePixel = 0
Instance.new("UICorner", espBtn).CornerRadius = UDim.new(0,6)

local autoBtn = Instance.new("TextButton", controlBar2)
autoBtn.Size = UDim2.new(0.48,0,1,0)
autoBtn.Position = UDim2.new(0.52,0,0,0)
autoBtn.BackgroundColor3 = Color3.fromRGB(45,45,50)
autoBtn.Text = "Auto Kill: OFF"
autoBtn.TextColor3 = Color3.new(1,1,1)
autoBtn.Font = Enum.Font.GothamBold
autoBtn.TextSize = 11
autoBtn.BorderSizePixel = 0
Instance.new("UICorner", autoBtn).CornerRadius = UDim.new(0,6)

local controlBar3 = Instance.new("Frame", Content)
controlBar3.Size = UDim2.new(1,0,0,32)
controlBar3.Position = UDim2.new(0,0,0,82)
controlBar3.BackgroundTransparency = 1

local freezeBtn = Instance.new("TextButton", controlBar3)
freezeBtn.Size = UDim2.new(0.48,0,1,0)
freezeBtn.BackgroundColor3 = Color3.fromRGB(45,45,50)
freezeBtn.Text = "Freeze: OFF"
freezeBtn.TextColor3 = Color3.new(1,1,1)
freezeBtn.Font = Enum.Font.GothamBold
freezeBtn.TextSize = 11
freezeBtn.BorderSizePixel = 0
Instance.new("UICorner", freezeBtn).CornerRadius = UDim.new(0,6)

local chestBtn = Instance.new("TextButton", controlBar3)
chestBtn.Size = UDim2.new(0.48,0,1,0)
chestBtn.Position = UDim2.new(0.52,0,0,0)
chestBtn.BackgroundColor3 = Color3.fromRGB(45,45,50)
chestBtn.Text = "Auto Chest: OFF"
chestBtn.TextColor3 = Color3.new(1,1,1)
chestBtn.Font = Enum.Font.GothamBold
chestBtn.TextSize = 11
chestBtn.BorderSizePixel = 0
Instance.new("UICorner", chestBtn).CornerRadius = UDim.new(0,6)

-- ==================== EVENTS & LOOPS ====================
refreshBtn.MouseButton1Click:Connect(function()
	getEn()
end)

killAllBtn.MouseButton1Click:Connect(function()
	killAllBtn.Text = "..."
	killAllBtn.BackgroundColor3 = Color3.fromRGB(180,50,50)
	killAll()
	killAllBtn.Text = "Kill All"
	killAllBtn.BackgroundColor3 = Color3.fromRGB(235,75,70)
end)

espBtn.MouseButton1Click:Connect(function()
	espOn = not espOn
	espBtn.Text = "ESP: " .. (espOn and "ON" or "OFF")
	espBtn.BackgroundColor3 = espOn and Color3.fromRGB(10,132,255) or Color3.fromRGB(45,45,50)
	if not espOn then
		for _, hl in pairs(hlts) do hl:Destroy() end hlts = {}
		for _, hl in pairs(playerHlts) do hl:Destroy() end playerHlts = {}
	end
end)

autoBtn.MouseButton1Click:Connect(function()
	autoOn = not autoOn
	autoBtn.Text = "Auto Kill: " .. (autoOn and "ON" or "OFF")
	autoBtn.BackgroundColor3 = autoOn and Color3.fromRGB(10,132,255) or Color3.fromRGB(45,45,50)
end)

freezeBtn.MouseButton1Click:Connect(function()
	frozen = not frozen
	freezeBtn.Text = "Freeze: " .. (frozen and "ON" or "OFF")
	freezeBtn.BackgroundColor3 = frozen and Color3.fromRGB(10,132,255) or Color3.fromRGB(45,45,50)
	freezeAll()
end)

chestBtn.MouseButton1Click:Connect(function()
	autoChestOn = not autoChestOn
	chestBtn.Text = "Auto Chest: " .. (autoChestOn and "ON" or "OFF")
	chestBtn.BackgroundColor3 = autoChestOn and Color3.fromRGB(10,132,255) or Color3.fromRGB(45,45,50)
end)

task.spawn(function()
	while true do
		task.wait(0.2)
		getEn()
		if autoOn then
			killAll()
		end
		processChests()
	end
end)
